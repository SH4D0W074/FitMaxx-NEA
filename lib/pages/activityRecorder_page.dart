import 'package:flutter/material.dart';

class ActivityrecorderPage extends StatelessWidget {
  const ActivityrecorderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}