import 'package:flutter/material.dart';

class WorkoutlogTab extends StatelessWidget {
  const WorkoutlogTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}