import 'package:flutter/material.dart';

class GpsrecorderTab extends StatelessWidget {
  const GpsrecorderTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GPS Recorder Tab'),
      ),
    );
  }
}